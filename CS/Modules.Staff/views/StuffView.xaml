<UserControl x:Class="Modules.Stuff.StuffView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:Modules.Stuff">
    <Border DataContext="{x:Static local:StuffView.Controller}">
    <ListBox ItemsSource="{Binding SelectedTeam.Stuff}" Background="{x:Null}">
        <ListBox.ItemTemplate>
            <DataTemplate>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding FirstName}" FontWeight="bold" />
                    <TextBlock Text="{Binding LastName}" FontWeight="bold" Margin="5,0,0,0" />
                    <TextBlock Text="{Binding JobTitle}" Margin="5,0,0,0" />
                    <TextBlock Text="icq" Margin="5,0,0,0" />
                    <TextBlock Text="{Binding Icq}" Margin="5,0,0,0" />
                </StackPanel>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</Border>
</UserControl>
