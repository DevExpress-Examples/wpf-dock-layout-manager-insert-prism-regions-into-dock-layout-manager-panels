<UserControl x:Class="Modules.TeamList.TeamListView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:Modules.TeamList">
    <StackPanel>
        <WrapPanel TextBlock.Foreground="Gray">
            <TextBlock Text="Team name" />
            <TextBlock Text="Team lead name" Margin="15,0,0,0" />
        </WrapPanel>
        <ListBox x:Name="list" ItemsSource="{x:Static local:TeamList.Teams}" SelectedIndex="0" Background="{x:Null}" SelectionChanged="list_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding TeamName}" />
                        <TextBlock Text="{Binding TeamLead.FirstName}" Margin="15,0,0,0" />
                        <TextBlock Text="{Binding TeamLead.LastName}" Margin="5,0,0,0" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>
</UserControl>
